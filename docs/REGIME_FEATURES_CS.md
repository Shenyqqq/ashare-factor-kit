# 已退役：CS Regime（`轮动_*`）

本设计（截面 `轮动_*` 注入 ML 因子矩阵）已移除。

请改读 **[POSITION_REGIME.md](POSITION_REGIME.md)**：市场级仓位体制信号 →
`target_exposure` → 回测敞口缩放（`--position-regime`）。

旧 CLI `--regime-cs` / `--no-regime` / `--ridge-drop-regime` 现为 no-op。
