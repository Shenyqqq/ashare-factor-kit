# ── 数据 ──────────────────────────────────────
akshare>=1.12.0          # A股行情/财务数据
tushare>=1.2.89          # 备用数据源（需注册token）
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0          # parquet 本地存储

# ── 因子计算 ───────────────────────────────────
scipy>=1.11.0
statsmodels>=0.14.0
ta>=0.11.0               # 技术指标

# ── 机器学习 ───────────────────────────────────
scikit-learn>=1.3.0
xgboost>=2.0.0
lightgbm>=4.1.0
catboost>=1.2.0
shap>=0.44.0
optuna>=3.5              # 离线超参搜索（LGBM/XGB/RF，--tune）

# ── 组合优化 ───────────────────────────────────
cvxpy>=1.4.0             # 凸优化（均值-方差/风险平价）

# ── 可视化 ────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.17.0

# ── 工具 ──────────────────────────────────────
python-dotenv>=1.0.0     # 管理 token/密钥
PyYAML>=6.0              # factor_configs.yaml 读写（run.py / logs/driver.py）
loguru>=0.7.0            # 更好用的日志
schedule>=1.2.0          # 定时任务
jupyter>=1.0.0
tqdm>=4.66.0
hmmlearn>=0.3.3
